package com.paradox543.malankaraorthodoxliturgica.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Brand colors for Malankara Orthodox Liturgica app
val Primary = Color(0xFF5D0000)       // Topbar background
val OnPrimary = Color(0xFFFFD700)     // Topbar text/icons
val PrimaryContainer = Color(0xFF7A1A1A) // Slightly lighter maroon for filled sections
val OnPrimaryContainer = Color(0xFFFFF3CC) // Soft gold/ivory text on primary container

val Secondary = Color(0xFFD4AF37)     // Optional secondary brand color
val OnSecondary = Color.Black
val SecondaryContainer = Color(0xF0FFDE8A) // Lighter gold tone for filled sections
val OnSecondaryContainer = Color(0xFF1A1A1A) // Dark text for contrast

// Backgrounds
val Background = Color(0xFFFAF8F2)    // App background (light mode)
val OnBackground = Color(0xFF1A1A1A)

// Dark Brand Colours for the app
val PrimaryDark = Color(0xFF8C1C1C)          // Rich maroon for topbar (slightly brighter for dark mode)
val OnPrimaryDark = Color(0xFFFFD966)        // Warm gold text/icons on topbar

val PrimaryContainerDark = Color(0xFF5D0000) // Deeper maroon for filled sections
val OnPrimaryContainerDark = Color(0xFFFFE8B0) // Soft gold text for readability

val SecondaryDark = Color(0xFFE6C76E)        // Muted gold tone for secondary brand color
val OnSecondaryDark = Color(0xFF1A1A1A)      // Dark text on gold

val SecondaryContainerDark = Color(0xFF8F7E27) // Dark gold-brown container
val OnSecondaryContainerDark = Color(0xFFFFEFC2) // Light ivory text

// Backgrounds
val BackgroundDark = Color(0xFF121212)       // Standard dark background
val OnBackgroundDark = Color(0xFFEAEAEA)     // Light text for contrast

val CardBorderColor = Color(0xFFDFDFDF)

val HintColor = Color(0xFFBDBDBD)
